
Global setup:

  Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email mohdzamrimurah@gmail.com
        

Next steps:

  mkdir first_course_in_database
  cd first_course_in_database
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:mohdzamri/first_course_in_database.git
  git push origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:mohdzamri/first_course_in_database.git
  git push origin master
      

Importing a SVN Repo?

  Click here
      

When you're done:

  Continue

